name: Validate private workbench

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Run privacy, secret, structure, and link checks
        run: bash scripts/audit-publication.sh
